# num = [1, 2, 3] >>> List
# num = (1, 2, 3) >>> Tuples

num = (1, 2, 3)
n = [1, 2, 3]
# print(num[0])

# x = num[0]
# y = num[1]
# z = num[2] it is normal in other language ,in python you should diff parameter

x, y, z = n
print(x)

# It is used both side in (List and Tuples)
